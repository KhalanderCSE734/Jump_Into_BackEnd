<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MULTER</title>
</head>
<body>
    <h1>Welcome to  MULTER</h1>
    <form action="/upload" method="POST" enctype="multipart/form-data">
        <input type="file" name="Image" id="fileId">
        <button type="submit" style="padding:10px;background-color:aquamarine;color:orangered;cursor:pointer;border:none;"> Upload File </button>
    </form>
    <% if (locals.msg) { %>
     <p> <%= msg %>  </p>
    <% } %>
    <% if (locals.src) { %>
        <p>Image Recieved</p>
        <img src="<%= src %>" alt="ScreenShot">
    <% } %>
</body>
</html> 